'use client';

import styles from './Header.module.scss';

function Header() {
    return (
        <header className={styles.header}>
            <div className={styles.container}>Header3333</div>
        </header>
    );
}

export default Header;
